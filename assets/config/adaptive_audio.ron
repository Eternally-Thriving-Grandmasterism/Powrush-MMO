# Adaptive Audio Configuration
# Includes Dynamic Bias Scaling + Priority Stacking parameters

(
    # ... existing ramp and reload fields ...

    # === Dynamic Bias (category-specific ducking strength) ===
    music_ducking_bias: 1.15,
    ambient_ducking_bias: 1.0,
    sfx_ducking_bias: 0.9,
    ui_ducking_bias: 0.6,

    # === Priority Stacking ===
    stacking_critical_per_sound: 0.08,
    stacking_high_per_sound: 0.06,
    max_stacking_reduction: 0.35,
)
