apiVersion: v1
kind: ConfigMap
metadata:
  name: powrush-config
  labels:
    app: powrush-mmo
data:
  RUST_LOG: "info"
  SERVER_HOST: "0.0.0.0"
  GAME_PORT: "7777"
  WS_PORT: "9001"
  HTTP_PORT: "8080"
  # Non-sensitive config only. Secrets go in Secret or .env