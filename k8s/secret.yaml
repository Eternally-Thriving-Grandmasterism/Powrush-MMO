apiVersion: v1
kind: Secret
metadata:
  name: powrush-secrets
  labels:
    app: powrush-mmo
type: Opaque
stringData:
  # Replace with your real key before apply. Never commit real keys to git.
  GROK_API_KEY: "sk-proj-YOUR_XAI_GROK_KEY_HERE"
  # Optional additional secrets