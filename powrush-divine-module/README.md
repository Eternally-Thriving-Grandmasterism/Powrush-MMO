# `powrush-divine-module/` — not a workspace member

This crate path-deps `../../../Ra-Thor/ra-thor-core`, which CI does not have.

Left on disk. Dropped from `[workspace].members` so `cargo metadata` works in this repo alone.

Do not restore the member until the path dep is gone or Ra-Thor is an explicit local overlay.

Contact: info@Rathor.ai
