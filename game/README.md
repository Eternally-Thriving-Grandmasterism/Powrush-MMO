# `game/` — not a workspace member

This directory is a loose `.rs` dump (no `Cargo.toml`).

**Lived first hour** is `client/src` (`PowrushClientBundle`). Do not add `game` back to `[workspace].members` until it has a standalone manifest that compiles without a sibling Ra-Thor tree, and without becoming a second player loop.

Contact: info@Rathor.ai
