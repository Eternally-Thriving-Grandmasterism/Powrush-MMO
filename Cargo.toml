[package]
name = "powrush-mmo"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.14"
bevy_kira_audio = "0.19"
rand = "0.8"
bevy_replicon = "0.26"  # Multiplayer sync
tokio = { version = "1", features = ["full"] }
