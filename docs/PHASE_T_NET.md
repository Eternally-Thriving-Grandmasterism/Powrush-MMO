# PHASE_T_NET.md — Honest net label (v23.2.42)

Default: `NetMode::Offline` — one human / one machine.

`HonestShard` is a labelled option. Peer count on Peace-hour boot is **never** invented.
`server/` stays parked in this slice (`server_unparked() == false`).

Real peer path is a later PR when the server is honestly ready.
