# NET_OFFLINE_CONTRACT.md — Online later; Offline is the door

**Contact:** info@Rathor.ai  
**Independent of xAI.** No certification / AGSi warranty / legal-product claims.

Phase **C1** — docs only. Names the contract. **Does not implement sockets.**  
Parent law: `docs/SIM_AND_HAND_CANON.md`. SKU: `docs/OFFLINE_SKU.md`. Protocol detail (parked): `docs/PROTOCOL.md`.

**Play tip:** `aabecfb` · **Law tip:** `3f80946` · Tag stays `11c577e`.

---

## 1. Title Online stays grey

Title Online stays **grey** until the steward writes **`online yes`**.

No docs stamp, LAN loopback, GenShare file, or postcard sketch lights that row.

---

## 2. Default run is Offline

- Default: `POWRUSH_NET` **unset** or **`off`**.
- `scripts/play-offline.sh` and `scripts/play-offline.ps1` already **force** `POWRUSH_NET=off`.
- Default binary opens **zero** sockets. No listen. No outbound WS.

---

## 3. Body is local F

WASD · look · Use (Peace E / South) run on the **local** FixedUpdate hand (`SIM_AND_HAND_CANON`).

**Never** lockstep the capsule pose across the net. Ice-skate / eaten E / Title hitch stay local hand bugs.

---

## 4. Shared later = small C only

When Online is named later, share **commands / digests**, not poses:

| Field family | Meaning |
|---|---|
| Verb | Tend / Flow / Take (where Use allows) |
| Place | hex id |
| Week digest | \(T_w\), \(U_w\) (same math as House week) |

Local body stays local. Shared climate digest may lag. Finger ack this tick; universe may be late.

---

## 5. Hello before peers · loopback first

| Law | Detail |
|---|---|
| Hello first | No peer list / presence before an honest **hello** path |
| Loopback first | **`127.0.0.1` only** for any lab listen / dial |
| Never | **`0.0.0.0`**, public bind, or “open a port to test” from this contract |

Settings LAN · loopback (if present) is a **lab**, not the SKU promise, and does **not** light Title Online.

---

## 6. One late peer must not freeze Sanctuary

A lagging stranger must **not** freeze the Offline yard. Sanctuary Peace remains playable under Offline authority. Classic RTS “wait on turn N for everyone” is wrong for Peace WASD (`SIM_AND_HAND_CANON` §5).

---

## 7. Postcard / varint later — names only

This file **names** later postcard / varint / digest fields. It does **not**:

- open a socket
- ship a binary envelope
- change `client/**` or implement net in `shared/**`

Optional later postcard sketch (names only, not wired here): house id · hex id · week \(T_w\)/\(U_w\) · climate mood token · protocol_rev. Detail stays in `PROTOCOL.md` until steward says **online yes**.

---

## 8. Market / lethal / wars = not this contract

| Out of scope | Why |
|---|---|
| Market Place | HOLD; not Offline 1.0 |
| Lethal / wars | Opt-in later; never default Peace E |
| Shard wars / factions | Sky / MMO later |

---

## 9. Ra-Thor reads ticks; does not drive keys

Lattice may **read** lived-tick / climate JSON when ingest is on. Ra-Thor **never** drives WASD / Use / Title. No path dep from the yard client.

---

## Refuse

`client/**` · `shared` net impl · Cargo bump · `POWRUSH_NET=on` · Title Online light · `0.0.0.0` · public bind · opening a port “to test” · `playable-preview` retag · child agent · Simulator merge · inventing Hour-two minutes

**Thunder locked in.** Yoi ⚡
