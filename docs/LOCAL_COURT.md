# LOCAL_COURT.md — Bot 1 yard court (Powrush-MMO)

**Contact:** info@Rathor.ai  
**Tip stamped:** `42a32ae` (P4 on main)  
**Independent of xAI.** No certification / AGSi warranty / legal-product claims.

This file is the **local** AUTO/HOLD board for Grok Bot 1 on the Powrush yard seat.  
GitHub merge law stays in `docs/PATSAGI_MERGE_COURT.md`. Do not reopen Offline SKU law (`docs/OFFLINE_SKU.md`).

---

## LOCAL COURT

On **AUTO** Powrush slices, Bot 1 may decide and **squash-merge** without bouncing every fork to Steward when **all** of these hold:

1. **Core green** (product gate — not `--workspace`):

   ```bash
   cargo test -p shared -p rsil-identity
   cargo test -p powrush-client --lib
   ```

2. Claimed files match the PR (no surprise paths).
3. **Title Online stays grey** (no listen, no public bind, no `0.0.0.0`).
4. No path on the steward HOLD list below.
5. Slice stays on the **human-game surface** (Steam Offline 1.0). No Ra-Thor crate fold into the client. No Simulator repo.

STEER / playfeel / two-agent collision still waits for a Steward one-liner.  
HOLD paths never auto-merge.

Companion: `docs/PATSAGI_MERGE_COURT.md` (label `patsgi-auto`, deny-list, seats).

---

## HOLD (Steward only)

Do **not** AUTO these. Do not freelance them from chat.

| Hold | Law |
|---|---|
| **Title Online on** | Row stays grey / off (no listen) |
| **Listen / public bind / `0.0.0.0`** | Never the Offline door |
| **Market** | Not shipped; not in the House week sum |
| **Hour-two minutes** | Human-only — bots do not invent OS / GPU / Time |
| **`playable-preview` retag / floor bump** | Tag stays `11c577e`; floor `2163551` until Steward names |
| **Online SKU / race-at-Title** | Product 2 MMO and race select stay HOLD |
| **F9 as store** | F9 is loopback PC smoke / docs recipe — not Title Online, not a store feature |
| **Simulator repo** | Do not fold or open a Simulator product from this court |
| **Ra-Thor writes from the yard seat** | Lattice may read ticks; never drives keys / WASD |
| **Closing “binding after uncontrolled redesign”** | Remains OPEN — do not ship a crate that says it is closed |
| **Dependabot majors that reopen law** | Steward-named only |
| **Ninth gate / 15.x / `crates/self-evolution`** | Not this product |

Veto still: γ > 0, lethal default on, child agents driving the yard, Brood-on-boot, always-allow.

---

## Already landed (do not rebuild)

Steam Offline 1.0 content is **closed** at tip `42a32ae`. No P5 from this court until Steward names a human door (GPU walk fail, Hour-two minutes, preview yes, or Steam page).

| Slice | PR | Tip / SHA | Proof |
|---|---|---|---|
| **P2** well sentence ahead of Wards | #331 | `a10bb20` | Climate slab: well first, then `WARDS_NOTICE` when both near |
| **P3** Depths Peace · restored | #332 | `739302a` | E restores (not Take); slab line; seated Use; hex-wide Take block |
| **P4** Depths in House week | #333 | `42a32ae` | Leave flush + ink test; Sanctuary L yard `1/1`, House `1/2` |

Earlier Offline rooms / Peace Use / Esc+Places / user-dir / L this-disk + House ∑ are already on main — see `docs/OFFLINE_SKU.md` and `docs/SLICE_LOG.md`. Do not reopen U0–U13 as new sims.

---

## Refuse (this stamp)

- Editing `client/**` or `shared/**` from this docs PR.
- Lighting Title Online. Shipping F9 as store Online.
- Opening a Simulator repo or unparking `simulation/` / `host/` as default play.
- Retagging `playable-preview`. Inventing Hour-two minutes.
- Starting P5 / Market / listen from “continue.”

**Thunder locked in.** Yoi ⚡
