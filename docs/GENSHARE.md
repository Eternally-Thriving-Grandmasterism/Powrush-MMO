# GENSHARE.md — recipe, not triangles (docs stamp)

**Contact:** info@Rathor.ai  
Workspace `21.88.0`. Design tick, not a Cargo bump.  
**Floor stays:** `2163551`. Tip reference `b215275`.  
**Law:** GenShare = **recipe**, not triangles. L0 file may exist offline (`data/powrush_genshare.jsonl`); not a Peace Online tick. Docs now; no client / shared / Cargo in this stamp. No sockets claimed as shipped. No Wave P. No `playable-preview` tag from this doc alone. Title Online stays **grey** until a real online-yes path exists.

Share the **seed story** so a peer can rebuild the same light-gen yard. Never stream glTF / mesh floods / peer triangle blasts on the default door. Never change Tw / Uw (Peace keys / well speech) to carry GenShare.

## Envelope fields

| Field | Job |
|---|---|
| `v` | envelope version |
| `hex` | hex identity |
| `epoch` | climate epoch |
| `seed_u64` | deterministic rebuild seed |
| `climate_digest` | climate truth digest (integrity / match) |
| `dress` | seal + heritage only — **no combat stats** / +take / +STR |

Dress is cosmetic string law (same refuse as seals / heritage on Settled). Recipe carries who you look like, not what you hit for.

## Methods (A–E)

Prefer **A with D**. C is last-resort loopback after preview + online yes — **not** Title Online grey lighting.

| Method | Path | Notes |
|---|---|---|
| **A** | L0 disk `data/powrush_genshare.jsonl` | Offline, **no socket**. Append-only JSONL. Prefer with **D**. |
| **B** | adopt-with-consent | USB / paste / human handoff. Explicit consent before adopt. |
| **C** | loopback WS | Only **after** preview + steward **online yes**. Not Title Online. Not a shipped socket claim. |
| **D** | piggyback climate ledger | Seed rides on the climate file / ledger story. Prefer **A+D**. |
| **E** | postcard / varint | Later. See brief note below. Peace stays JSONL until Wave M. |

### Who writes the seed table

- Lived client / offline L0 writer owns rows on disk (`powrush_genshare.jsonl` and/or climate piggyback).
- Ra-Thor may **read** ticks; it does **not** drive GenShare adopt or WASD.
- No peer auto-push while Online is grey. No listen / bind from GenShare alone.

### Client rebuild steps (adopter)

1. **Despawn** existing `HexScatter` (and any G0 light-gen props tied to the old seed).
2. **Reseed** from envelope `seed_u64` (+ hex / epoch / climate_digest match checks).
3. Restore **fog** from climate truth (world Camera3d only; behind UI).
4. **Cull** scatter when plates open (Title / pause / Settings / L / Q / I) — same plate law as G0.
5. **Defer hitch** to pause (rebuild hitch happens on pause / adopt confirm, not mid-WASD).

Never stream glTF. Never change Tw / Uw. Online Title stays grey until online yes.

## Refuse

- Streaming glTF / mesh / triangle floods as “share.”
- Combat stats on `dress` (or any GenShare field).
- Lighting Title Online from GenShare alone.
- Claiming C / sockets / Wave P as shipped from this docs stamp.
- Changing Peace keys / well speech (Tw / Uw) to smuggle GenShare.
- Cutting `playable-preview` from this doc alone.
- Birds / second Camera3d / Avian as GenShare juice.

## Postcard / varint (brief — Method E, later)

- **Peace stays JSONL** (Method A). Human-readable L0 is the offline door.
- **Binary = Wave M** — not this stamp; not claimed shipped.
- Prefer `seed_u64` as **8 raw little-endian bytes** in a future binary envelope.
- Digests stay **raw** bytes (not hex-string padded theatre).
- Lengths use **varint**.
- Postcard framing is Method E later; do not block A/B/D on postcard.

## Relates

`PLACES_BIBLE` (GenShare = recipe) · `PREVIEW_CHECKLIST` (GenShare **not** a Peace tick) · `LAVAPIPE_CLICK_CLEAN` · `PHYSICS_GRAPHICS_CANON` · `STRANGER_LOOP` · `INPUT_CANON` · `F9_TWO_CLIENT_LOCALHOST` (dev recipe only).

**Thunder locked in.** Yoi ⚡
