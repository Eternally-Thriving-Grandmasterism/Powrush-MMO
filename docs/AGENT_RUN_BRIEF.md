# Agent run brief

**Workspace:** 21.88.0  
**Design tick:** as README (not a Cargo bump)  
**Contact:** info@Rathor.ai  
**Pattern:** Ra-Thor `docs/AGENT_RUN_BRIEF.md` (merged #504 / `bf44300a`), adapted to this seat. The **outer loop does not write code**. This file is the inner-loop contract.

Cursor / PATSAGi inner-loop agents start here. Fetch the named tip. Fill every section. Then implement **one slice**. Do not interview. Do not clone or edit Ra-Thor. Do not start a lattice seat from this repo.

---

## A. Worked example — VALENCE-HUD-2 (this run)

Seat: **VALENCE-HUD-2**. Mode: **routine**. Tip: `6b7d1812` (F4-GENSHARE-ADOPT-CONSENT on `main`).

### 1. Target repo + tip SHA

- Repo: https://github.com/Eternally-Thriving-Grandmasterism/Powrush-MMO
- Tip SHA (fetch first): `6b7d181292ffb414f1ce7047610539395444b6a8` (short `6b7d1812`)
- Workspace identity: **21.88.0** (Cargo.toml). Lived-hour design tick as README — not a Cargo bump.

### 2. Job in one sentence

Add a second visible NEVC HUD channel: raw event alignment / craft labeled **stewardship quality**, display only. Do not retune the kernel.

### 3. FINISH LINE

Observable:

1. Lived HUD helper (`nevc_visibility`) keeps **stewardship / harm gate** as the class score (`NevcSummary.score`).
2. Same helper shows raw alignment / craft from the event (`RbeAction.abundance_alignment` or `RawSample.valence`) labeled **stewardship quality** — display only; it does not feed class or recovery.
3. Class bit unchanged. Compassion-gate recovery stays Open on the existing floor-lift / grief-gate cases.
4. Core gate green:
   `cargo test -p shared -p rsil-identity`
   `cargo test -p powrush-client --lib`
5. Title Online grey; no gold; no Embassy; no Ra-Thor WASD; no `v_min` / `valence_floor` rewrite.

States that hold: Title Online grey; `playable-preview` stays `11c577e`; floor `2163551`; Ra-Thor does not drive WASD; Powrush-MMO-Simulator is a third repo.

### 4. Out of scope (explicit)

- Do not retune `v_min` / `valence_floor`, `positive_weight`, or the harvest grief table.
- Do not add gold, Title Online, Embassy, Wave C, `.glb`.
- Do not rebuild U0–U8, H-2026-09-15-H2-TAB/Q/L/RESUME, H3-FAB, H3-SEAT, BRIEF-P1, PLAYTEST-1, NEVC-HONEST-1.
- No Title Online · no listen · no public bind · no Steam retag.
- Do not clone or edit Ra-Thor. **Ra-Thor is a separate repo.**
- Two agents, one file, same day → **STOP**.

### 5. HOLD

Copy unless the steward amends:

- `AGENTS.md` · `docs/PATSAGI_MERGE_COURT.md` · `docs/OFFLINE_SKU.md`
- `docs/HOUR_TWO.md` · `docs/HOUR_TWO_PLAYTEST.md` · `docs/HOUR_THREE.md`
- Violence opt-in only. Purse = flow + repair-rights.
- Independent of xAI · inspect ≠ METR · RBE is design intent, not present fact
- Default binary: `cargo run -p powrush-client`
- Title Online stays grey · `playable-preview` stays `11c577e` · floor `2163551`
- Workspace **21.88.0** (not a Cargo bump; design tick as README)
- Contact: info@Rathor.ai
- Ra-Thor does not drive WASD
- Powrush-MMO-Simulator is a third repo

### 6. Tools allowed

- GitHub (read, branch, one PR).
- `cargo` from root [`Cargo.toml`](../Cargo.toml) members only: `shared`, `crates/rsil-identity`, `client`.
- HTML/JS/CSS as named: **none**.

### 7. Permission rules

- Never claim METR / xAI product / RBE-as-present-fact.
- Never send email or publish.
- Never enable Title Online, add a listen, or public-bind.
- Never retag `playable-preview`. Never unpark `server/` as the default binary.
- One slice per PR. Fill the ballot in [`.github/pull_request_template.md`](../.github/pull_request_template.md).

### 8. Mode

**routine** — execute this named slice. Not `chat`. Not a standing Hour-3 or Hour-4 agent.

### 9. STOP / review gate

Halt when the PR `feat(hud): VALENCE-HUD-2 stewardship quality channel` contains the second visible channel on the lived HUD helper, Core green, class/recovery unchanged, and the ballot.

Do **not** retune the kernel. Do **not** add gold / Title Online / Embassy / Wave C / `.glb`.

Human review: PATSAGi merge court. Steward contact remains info@Rathor.ai.

### 10. Audit note (what would retire this agent)

VALENCE-HUD-2 retires at merge. Further work is a **new** brief, not this seat continuing. A kernel retune, Title Online light, Steam retag, `server/` unpark, or Ra-Thor edit would be a different agent — refuse it here.

Capable · Bounded · Corrigible. Not METR. Thunder locked. yoi ⚡

---

## A0. Previous example — NEVC-HONEST-1 (merged #451)

Seat: **NEVC-HONEST-1**. Mode: **routine**. Tip: `8bc591d7` (TEST-PEACE-DEFAULT on `main`). Merged `1d3aa6d1`. Keep as the grief-gate copy example; do not rebuild.

### 1. Target repo + tip SHA

- Repo: https://github.com/Eternally-Thriving-Grandmasterism/Powrush-MMO
- Tip SHA (fetch first): `8bc591d714636d0fadbd2ee24c79a5920df9095e` (short `8bc591d7`)
- Workspace identity: **21.88.0** (Cargo.toml). Lived-hour design tick as README — not a Cargo bump.

### 2. Job in one sentence

Make the living HUD/docs tell the truth Grok stated: NEVC class is a grief-gate teaching signal, not wages. Do not retune the kernel.

### 3. FINISH LINE

Observable:

1. README (the living doc that names NEVC / Contributor / Zombie) has one paragraph: `RbeAction` lifts alignment onto the floor; grief decides class; recovery is Open; harvest teaching signal, not wages.
2. Ledger help (`ContributionEvent::RbeAction`) says the same: alignment is not an abundance score.
3. Lived HUD helper (`nevc_visibility`) labels the numeric field **stewardship / harm gate**, not “abundance score”; no new HUD; no kernel retune (`valence_floor` / `positive_weight` / harvest grief table untouched).
4. Core gate green:
   `cargo test -p shared -p rsil-identity`
   `cargo test -p powrush-client --lib`
5. Title Online grey; tap-E / hold-E / R flow-reserve unchanged.

States that hold: Title Online grey; `playable-preview` stays `11c577e`; floor `2163551`; Ra-Thor does not drive WASD; Powrush-MMO-Simulator is a third repo.

### 4. Out of scope (explicit)

- Do not retune `v_min` / `valence_floor`, `positive_weight`, or the harvest grief table.
- Do not add gold, Title Online, Embassy, Wave C, `.glb`.
- Do not rebuild U0–U8, H-2026-09-15-H2-TAB/Q/L/RESUME, H3-FAB, H3-SEAT, BRIEF-P1, PLAYTEST-1.
- No Title Online · no listen · no public bind · no Steam retag.
- Do not clone or edit Ra-Thor. **Ra-Thor is a separate repo.**
- Two agents, one file, same day → **STOP**.

### 5. HOLD

Copy unless the steward amends:

- `AGENTS.md` · `docs/PATSAGI_MERGE_COURT.md` · `docs/OFFLINE_SKU.md`
- `docs/HOUR_TWO.md` · `docs/HOUR_TWO_PLAYTEST.md` · `docs/HOUR_THREE.md`
- Violence opt-in only. Purse = flow + repair-rights.
- Independent of xAI · inspect ≠ METR · RBE is design intent, not present fact
- Default binary: `cargo run -p powrush-client`
- Title Online stays grey · `playable-preview` stays `11c577e` · floor `2163551`
- Workspace **21.88.0** (not a Cargo bump; design tick as README)
- Contact: info@Rathor.ai
- Ra-Thor does not drive WASD
- Powrush-MMO-Simulator is a third repo

### 6. Tools allowed

- GitHub (read, branch, one PR).
- `cargo` from root [`Cargo.toml`](../Cargo.toml) members only: `shared`, `crates/rsil-identity`, `client`.
- HTML/JS/CSS as named: **none**.

### 7. Permission rules

- Never claim METR / xAI product / RBE-as-present-fact.
- Never send email or publish.
- Never enable Title Online, add a listen, or public-bind.
- Never retag `playable-preview`. Never unpark `server/` as the default binary.
- One slice per PR. Fill the ballot in [`.github/pull_request_template.md`](../.github/pull_request_template.md).

### 8. Mode

**routine** — execute this named slice. Not `chat`. Not a standing Hour-3 or Hour-4 agent.

### 9. STOP / review gate

Halt when the PR `docs(hour): NEVC-HONEST-1 grief-gate copy` contains the README paragraph, ledger-help rustdoc, Core green, and the ballot.

Do **not** retune the kernel. Do **not** add gold / Title Online / Embassy / Wave C / `.glb`.

Human review: PATSAGi merge court. Steward contact remains info@Rathor.ai.

### 10. Audit note (what would retire this agent)

NEVC-HONEST-1 retires at merge. Further work is a **new** brief, not this seat continuing. A kernel retune, Title Online light, Steam retag, `server/` unpark, or Ra-Thor edit would be a different agent — refuse it here.

Capable · Bounded · Corrigible. Not METR. Thunder locked. yoi ⚡

---

## A0. Previous example — PLAYTEST-1 (merged #446)

Seat: **PLAYTEST-1**. Mode: **routine**. Tip: `03a0c6e0` (BRIEF-P1 #445 on `main`). Merged `258da81d`. Keep as the stranger-hour note example; do not rebuild.

### 1. Target repo + tip SHA

- Repo: https://github.com/Eternally-Thriving-Grandmasterism/Powrush-MMO
- Tip SHA (fetch first): `03a0c6e0a8e72a2ae2edf6facf13b7d59f8a2dc6` (short `03a0c6e0`, BRIEF-P1)
- Workspace identity: **21.88.0** (Cargo.toml). Lived-hour design tick as README — not a Cargo bump.

### 2. Job in one sentence

Lock the lived hour for a stranger in the existing first-hour playtest note (extend, do not fork doctrine).

### 3. FINISH LINE

Observable:

1. [`docs/FIRST_HOUR_PLAYTEST.md`](FIRST_HOUR_PLAYTEST.md) is the PLAYTEST-1 stranger hour note:
   - One-block `cargo test` then `cargo run -p powrush-client`
   - Hour 1 verbs: walk, breath, tap-E take vs hold-E tend, I satchel, R 1 flow vs R 2 reserve
   - Hour 2 yard-remembers verify points at [`HOUR_TWO_PLAYTEST.md`](HOUR_TWO_PLAYTEST.md) (already shipped; do not rebuild)
   - Report fields OS / GPU / commit / minutes / confusion stay **BLANK**
   - Success: stranger take → tend → allocate in <20 min without a wiki
2. No new currency, pockets, fake “players online”, CBDC-style lock, or kill-score.
3. Core gate green (docs-only; cite, do not rebuild PATHS):
   `cargo test -p shared -p rsil-identity`
   `cargo test -p powrush-client --lib`

States that hold: Title Online grey; `playable-preview` stays `11c577e`; floor `2163551`; Ra-Thor does not drive WASD; Powrush-MMO-Simulator is a third repo.

Playtest: a human fills the Report on a real GPU. Agents do not invent minutes.

### 4. Out of scope (explicit)

- Do not rebuild U0–U8, H-2026-09-15-H2-TAB/Q/L/RESUME, H3-FAB, H3-SEAT, BRIEF-P1.
- No Embassy / Crownstone / Hybrid / myth systems from this seat.
- No Title Online · no listen · no public bind · no Steam retag.
- No new currency · no pockets · no fake peers · no CBDC lock · no kill-score.
- No cargo workspace member change · no `server/` unpark.
- Do not clone or edit Ra-Thor. **Ra-Thor is a separate repo.**
- Two agents, one file, same day → **STOP**.

Walked PATHS (README Agent landing + Hour 2/3 stamps) — do not reopen: U0–U8, #309, HOUR1-GREEN, H2-TAB/Q/L/RESUME, H3-FAB, H3-SEAT `edbdf513`, BRIEF-P1 `03a0c6e0`.

### 5. HOLD

Copy unless the steward amends:

- `AGENTS.md` · `docs/PATSAGI_MERGE_COURT.md` · `docs/OFFLINE_SKU.md`
- `docs/HOUR_TWO.md` · `docs/HOUR_TWO_PLAYTEST.md` · `docs/HOUR_THREE.md`
- Violence opt-in only. Purse = flow + repair-rights.
- AG-SML: personal play free; org use licensed — do not invent in-world cash the software cannot pay.
- Independent of xAI · inspect ≠ METR · RBE is design intent, not present fact
- Default binary: `cargo run -p powrush-client`
- Title Online stays grey · `playable-preview` stays `11c577e` · floor `2163551`
- Workspace **21.88.0** (not a Cargo bump; design tick as README)
- Contact: info@Rathor.ai
- Ra-Thor does not drive WASD
- Powrush-MMO-Simulator is a third repo

### 6. Tools allowed

- GitHub (read, branch, one PR).
- `cargo` from root [`Cargo.toml`](../Cargo.toml) members only: `shared`, `crates/rsil-identity`, `client`.
- HTML/JS/CSS as named: **none**.

### 7. Permission rules

- Never claim METR / xAI product / RBE-as-present-fact.
- Never send email or publish.
- Never enable Title Online, add a listen, or public-bind.
- Never retag `playable-preview`. Never unpark `server/` as the default binary.
- One slice per PR. Fill the ballot in [`.github/pull_request_template.md`](../.github/pull_request_template.md).

### 8. Mode

**routine** — execute this named slice. Not `chat`. Not a standing Hour-3 or Hour-4 agent.

### 9. STOP / review gate

Halt when the PR `docs(hour): PLAYTEST-1 stranger hour note` contains the stranger hour note in `docs/FIRST_HOUR_PLAYTEST.md`, Core green, blank Report fields, and the ballot.

Do **not** start Hour 3 systems. Do **not** fill Report OS / GPU / minutes. Do **not** add Embassy / Crownstone / Hybrid / myth.

Human review: PATSAGi merge court. Steward contact remains info@Rathor.ai.

### 10. Audit note (what would retire this agent)

PLAYTEST-1 retires at merge. Further work is a **new** brief, not this seat continuing. Hour 3 systems wait for a human Hour-two Report. A Title Online light, Steam retag, `server/` unpark, Ra-Thor edit, or Crownstone PATH would be a different agent — refuse it here.

Capable · Bounded · Corrigible. Not METR. Thunder locked. yoi ⚡

---

## A0. Previous example — BRIEF-P1 (merged #445)

Seat: **BRIEF-P1**. Mode: **routine**. Tip: `edbdf513` (H-2026-09-15-H3-SEAT on `main`). Merged `03a0c6e0`. Keep as the worked test-lock example; do not rebuild.

### 1. Target repo + tip SHA

- Repo: https://github.com/Eternally-Thriving-Grandmasterism/Powrush-MMO
- Tip SHA (fetch first): `edbdf513862a2b56b3ba5dc19826b80c326347fe` (short `edbdf513`, H3-SEAT)
- Workspace identity: **21.88.0** (Cargo.toml). Lived-hour design tick as README — not a Cargo bump.

### 2. Job in one sentence

Land a Powrush inner-loop brief + lock the ALREADY WALKED Hour 2/3 verbs in lib tests so a human only fills OS/GPU/minutes.

### 3. FINISH LINE

Observable:

1. This file exists with sections 1–10 as a completed BRIEF-P1 example **and** a blank template.
2. `AGENTS.md` pointer: inner-loop agents start at `docs/AGENT_RUN_BRIEF.md`.
3. Core `--lib` tests already assert HOUR_TWO_PLAYTEST fail conditions (cite, do not rebuild PATHS):
   - Tab without allocate stays Peace — `try_ridge_tab` `client/src/hour_sacred.rs:127` · `tab_without_allocate_stays_peace` `:296` · `tab_key_without_allocate_stays_peace` `:370`
   - Q in Peace does not found a House — `try_plant_house` `client/src/hour_sacred.rs:138` · `q_on_peace_does_not_found` `:412`
   - L in Peace does not open Ledger — `try_toggle_ledger` `client/src/ledger_bind.rs:140` · `peace_keeps_ledger_closed` `:495` · `l_key_in_peace_does_not_open_ledger` `:513`
   - E on visitor ridge is *Not your charter* (no harvest) — `hour_two_line` `shared/space_law.rs:205` · `try_ridge_tab` visitor line `client/src/hour_sacred.rs:307` · refuse `handle_interact_harvest` `client/src/first_harvest_epiphany.rs:356`
   - Settled pack round-trips House + Settled in `data/powrush_hour_two.json` — `HOUR_TWO_PATH` `client/src/hour_sacred.rs:24` · `bind_not_option_three_marks_settled` `shared/hour_two.rs:137` · `hour_two_held_roundtrip` `shared/stranger_loop_proof.rs:95`
   - `resume_from_pack` skips WASD / Welcome back line exists — `resume_from_pack` `client/src/first_session_guidance.rs:176` · `welcome_line` `client/src/hour_two_resume.rs:24` · `h2_resume_does_not_reteach_wasd` `:615` · `h2_resume_welcome_names_the_yard` `client/src/hour_two_resume.rs:97`
4. Core gate green:
   `cargo test -p shared -p rsil-identity`
   `cargo test -p powrush-client --lib`
5. `docs/HOUR_TWO_PLAYTEST.md` Report fields (OS/GPU/minutes) stay **BLANK**. Do not invent a human walk.

States that hold: Title Online grey; `playable-preview` stays `11c577e`; floor `2163551`; Ra-Thor does not drive WASD; Powrush-MMO-Simulator is a third repo.

Playtest: a human fills the Hour-two Report on a real GPU. Agents do not invent minutes.

### 4. Out of scope (explicit)

- Do not rebuild U0–U8, H-2026-09-15-H2-TAB/Q/L/RESUME, H3-FAB, H3-SEAT.
- No Title Online · no listen · no public bind · no Steam retag.
- No Crownstone / Sylvaris / Hybrid / G Voice / DeclaredLethal / War week.
- No second HUD · no Depths boot · no Market · no NFT/k8s/payments.
- No cargo workspace member change · no `server/` unpark.
- No Ra-Thor HTML · no 23-pack essays.
- Do not clone or edit Ra-Thor. **Ra-Thor is a separate repo.**
- Two agents, one file, same day → **STOP**.

Walked PATHS (README Agent landing + today’s Hour 2/3 stamps) — do not reopen:

- U0 docs #298 `65458c4` · U1 user-dir #299 `a5755e2` · U2 hex travel #300 `5c754ad` · U2 book-survives #301 `0121433`
- U3 lamp spatial #302 `76adebe` · U4 audio #303 `0b73734` · U5 Deck Title #304 `789529e`
- U6 Heartwood lip #305 `6759b80` · U7 Threshold #307 `c269a89` · U8 Wards #308 `11bc62ee`
- #306 Cloud Agent env · #309 HourTwoHeld + NetMode `25aaf575`
- H-2026-09-15-HOUR1-GREEN `15691772` · H2-TAB `412300de` · H2-Q `d65dd3d6` · H2-L `7e548854`
- H2-RESUME `e03c400d` · H3-FAB `85a81dfd` · H3-SEAT `edbdf513`

### 5. HOLD

Copy unless the steward amends:

- `AGENTS.md` · `docs/PATSAGI_MERGE_COURT.md` · `docs/OFFLINE_SKU.md`
- `docs/HOUR_TWO.md` · `docs/HOUR_TWO_PLAYTEST.md` · `docs/HOUR_THREE.md`
- Independent of xAI · inspect ≠ METR · RBE is design intent, not present fact
- Default binary: `cargo run -p powrush-client`
- Title Online stays grey · `playable-preview` stays `11c577e` · floor `2163551`
- Workspace **21.88.0** (not a Cargo bump; design tick as README)
- Contact: info@Rathor.ai
- Ra-Thor does not drive WASD
- Powrush-MMO-Simulator is a third repo

### 6. Tools allowed

- GitHub (read, branch, one PR).
- `cargo` from root [`Cargo.toml`](../Cargo.toml) members only: `shared`, `crates/rsil-identity`, `client`.
- HTML/JS/CSS as named: **none**.

### 7. Permission rules

- Never claim METR / xAI product / RBE-as-present-fact.
- Never send email or publish.
- Never enable Title Online, add a listen, or public-bind.
- Never retag `playable-preview`. Never unpark `server/` as the default binary.
- One slice per PR. Fill the ballot in [`.github/pull_request_template.md`](../.github/pull_request_template.md).

### 8. Mode

**routine** — execute this named slice. The file is the handoff vehicle for later seats. Not `chat`. Not a standing Hour-4 Crownstone agent.

### 9. STOP / review gate

Halt when the PR `test(hour): BRIEF-P1 lock walked Hour 2/3 verbs` contains this file, the `AGENTS.md` pointer, Core green, and the ballot citing the existing fail-condition tests.

Do **not** invent Hour 4. Do **not** fill `docs/HOUR_TWO_PLAYTEST.md` Report fields. If every fail condition is already asserted, a docs-only PR (brief + pointer + ballot) is a valid finish.

Human review: PATSAGi merge court. Steward contact remains info@Rathor.ai.

### 10. Audit note (what would retire this agent)

BRIEF-P1 retires at merge. Further work is a **new** brief, not this seat continuing. Next inner loop (Hour 4 Crownstone Witness) needs a NEW brief **after** a human fills the HOUR_TWO_PLAYTEST Report. A Title Online light, Steam retag, `server/` unpark, Ra-Thor edit, or Crownstone PATH would be a different agent — refuse it here.

Capable · Bounded · Corrigible. Not METR. Thunder locked. yoi ⚡

---

## B. Template (copy for the next seat)

Paste a new “worked example” **above** this template (or replace section A). Do not start code until 1–10 are filled. The **outer loop does not write code**.

### 1. Target repo + tip SHA

- Repo: https://github.com/Eternally-Thriving-Grandmasterism/Powrush-MMO
- Tip SHA (fetch first):
- Workspace identity: 21.88.0 unless the steward named a Cargo bump. Design tick as README.

### 2. Job in one sentence

- 

### 3. FINISH LINE

- Observable (file:line, test name, or playtest step):

### 4. Out of scope (explicit)

- 
- Remember: **Ra-Thor is a separate repo.** Do not clone or edit it from this seat.
- Remember: **Powrush-MMO-Simulator is a third repo.** Do not fold the player loop into either sibling.
- Two agents, one file, same day → STOP.

### 5. HOLD

Copy unless the steward amends:

- AGENTS.md · docs/PATSAGI_MERGE_COURT.md · docs/OFFLINE_SKU.md
- docs/HOUR_TWO.md · docs/HOUR_TWO_PLAYTEST.md · docs/HOUR_THREE.md
- Independent of xAI · inspect ≠ METR · RBE is design intent, not present fact
- Default binary: cargo run -p powrush-client
- Title Online grey · playable-preview stays 11c577e · floor 2163551
- workspace 21.88.0
- info@Rathor.ai
- Ra-Thor does not drive WASD

### 6. Tools allowed

- GitHub
- cargo from root Cargo.toml
- HTML/JS/CSS as named: (list files or write “none”)

### 7. Permission rules

- Never claim METR / xAI product / RBE-as-present-fact
- Never send email or publish
- Never enable Title Online / listen / public bind
- One slice per PR

### 8. Mode

- [ ] chat
- [ ] routine
- [ ] handoff

### 9. STOP / review gate

- Halt when:
- Do not continue into:

### 10. Audit note (what would retire this agent)

- 

**Contact:** info@Rathor.ai  
Capable · Bounded · Corrigible. Not METR. Thunder locked. yoi ⚡
