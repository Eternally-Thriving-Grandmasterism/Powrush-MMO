# PHASE_LETHAL.md — Ledger 3 DeclaredLethal (v23.2.44) + L1 hex sign

Opt-in only after **Settled + Hour three held**. Confirm lives on Settings or the existing Q/Ledger face: **this hex admits harm** (default **off**). Digit3 on the open Ledger still confirms. Reuses `ShardStanding::declare_lethal` / `clear_lethal` — no second flag. Never default E. A sign, not a weapon.

## Effects
- standing: harmony ↓ peace ↓ consumption ↑; `declared_lethal = true`; tariff_paid += 1
- climate: stress ↑ harmony ↓; spend 1 reserve_pool else 1 restored_count
- Ledger purse blood_tariff = 2 (not refunded on clear)
- Missing / unknown `declared_lethal` → off. Stranger boot stays lethal off.

## Clear
Second Digit3 / Settings confirm → sign off. Tariff already paid stays on the week.

## Peace
Without charter skin, Ledger sash stays closed. No lethal UI on Peace-hour boot. Confirm without Settled + book does nothing; plate says *the ledger waits* / *Not your charter*.

## Week
Still **tons + restored**. Kills are not a score. Declare does not mint tons.
