{
  "name": "Powrush-MMO",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "xvfb-display",
      "command": "export XDG_RUNTIME_DIR=/tmp/xdg-runtime && mkdir -p $XDG_RUNTIME_DIR && chmod 700 $XDG_RUNTIME_DIR && exec Xvfb :99 -screen 0 1280x800x24 -ac +extension GLX +render -noreset"
    }
  ]
}
