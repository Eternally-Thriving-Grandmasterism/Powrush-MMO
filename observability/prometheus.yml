# observability/prometheus.yml
# Prometheus scrape configuration for Powrush-MMO

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
